/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'lzvyzfwbsssvofrjbndx.supabase.co' }
    ]
  }
}

export default nextConfig
